#pragma once

// Stub for future in-game menu (mode selection, settings, etc.)
// Intended to be fleshed out once the menu UI is designed.
class Menu {
public:
  void show()   {}
  void hide()   {}
  bool isOpen() { return false; }
};
